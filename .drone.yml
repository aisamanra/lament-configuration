kind: pipeline
name: default

steps:
- name: setup
  image: gdritter/lament-configuration-env:latest
  commands:
  - poetry install

- name: format
  image: gdritter/lament-configuration-env:latest
  commands:
  - inv checkfmt

- name: typecheck
  image: gdritter/lament-configuration-env:latest
  commands:
  - inv tc

- name: test
  image: gdritter/lament-configuration-env:latest
  commands:
  - inv test
