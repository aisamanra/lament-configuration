kind: pipeline
name: default

workspace:
  path: /root

steps:
- name: setup
  image: python
  environment:
    PIP_TARGET: '/root/.pip'
  commands:
  - pip install poetry
  - pip install invoke
  - poetry config virtualenvs.in-project true --local
  - poetry install

- name: test
  image: python
  environment:
    PIP_TARGET: '/root/.pip'
  commands:
  - inv test

- name: typecheck
  image: python
  environment:
    PIP_TARGET: '/root/.pip'
  commands:
  - poetry run inv tc

- name: format
  image: python
  environment:
    PIP_TARGET: '/root/.pip'
  commands:
  - poetry run inv checkfmt
