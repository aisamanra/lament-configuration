kind: pipeline
name: default

workspace:
  path: /root

steps:
- name: setup
  image: python
  commands:
  - pip install poetry
  - poetry run inv test
  - poetry run inv tc
  - poetry run inv checkfmt
